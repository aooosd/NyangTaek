using UnityEngine;
using TMPro;
public class TabCat : MonoBehaviour
{
    public string title;
    public TextMeshProUGUI titleText;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        title = "°í¾çÀÌ";
        //titleText = title;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
